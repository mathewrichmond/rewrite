<%= form_for(@insertion) do |f| %>
  <% if @insertion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@insertion.errors.count, "error") %> prohibited this insertion from being saved:</h2>

      <ul>
      <% @insertion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :review_id %><br>
    <%= f.number_field :review_id %>
  </div>
  <div class="field">
    <%= f.label :position %><br>
    <%= f.number_field :position %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
